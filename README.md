# PrivateVault × gstack

gstack agents decide what to do.  
PrivateVault decides whether it is allowed.

Example:
- Agents agree → execution still blocked
- Control layer before real-world impact

Run:
python gstack_real_flow.py
